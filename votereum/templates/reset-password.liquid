<!DOCTYPE html>
<html lang="en" xmlns:v="urn:schemas-microsoft-com:vml" xmlns:o="urn:schemas-microsoft-com:office:office">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="x-apple-disable-message-reformatting">
  <title>Reset Your Votereum Password</title>
  
  <!--[if mso]>
  <xml>
    <o:OfficeDocumentSettings>
      <o:PixelsPerInch>96</o:PixelsPerInch>
      <o:AllowPNG/>
    </o:OfficeDocumentSettings>
  </xml>
  <![endif]-->
  
  <style>
    /* Base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      height: 100% !important;
      width: 100% !important;
      background-color: #f8f9fc;
      margin: 0;
      padding: 0;
    }
    
    /* Typography */
    h1 {
      font-size: 28px;
      font-weight: 700;
      color: #2a2a72;
      margin-bottom: 16px;
    }
    
    p {
      font-size: 16px;
      line-height: 1.6;
      color: #4a4a4a;
      margin-bottom: 24px;
    }
    
    /* Container */
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
      box-shadow: 0 8px 24px rgba(10, 10, 120, 0.1);
      border-radius: 16px;
      overflow: hidden;
    }
    
    /* Header */
    .header {
      background: linear-gradient(135deg, #2a2a72 0%, #5050c5 100%);
      padding: 30px;
      text-align: center;
      position: relative;
    }
    
    .logo {
      width: 180px;
      margin-bottom: 20px;
    }
    
    /* Animation for header particles */
    @keyframes float {
      0% { transform: translateY(0) rotate(0deg); opacity: 1; }
      100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
    }
    
    .particle {
      position: absolute;
      height: 10px;
      width: 10px;
      border-radius: 50%;
      background-color: rgba(255, 255, 255, 0.3);
      animation: float 5s infinite linear;
    }
    
    .particle:nth-child(1) {
      left: 10%;
      top: 20%;
      animation-delay: 0s;
    }
    
    .particle:nth-child(2) {
      left: 20%;
      top: 80%;
      animation-delay: 1s;
    }
    
    .particle:nth-child(3) {
      left: 50%;
      top: 60%;
      animation-delay: 2s;
    }
    
    .particle:nth-child(4) {
      left: 80%;
      top: 30%;
      animation-delay: 3s;
    }
    
    .particle:nth-child(5) {
      left: 90%;
      top: 70%;
      animation-delay: 4s;
    }
    
    .header-text {
      color: white;
      font-size: 32px;
      margin-bottom: 10px;
      animation: fadeIn 1.5s ease-in-out;
    }
    
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    /* Content */
    .content {
      padding: 40px 30px;
      text-align: left;
    }
    
    /* Button */
    .button {
      display: inline-block;
      background: linear-gradient(135deg, #2a2a72 0%, #5050c5 100%);
      color: white;
      font-weight: bold;
      text-decoration: none;
      padding: 16px 32px;
      border-radius: 8px;
      margin: 20px 0;
      text-align: center;
      transition: transform 0.3s, box-shadow 0.3s;
      box-shadow: 0 4px 10px rgba(80, 80, 197, 0.3);
    }
    
    .button:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 15px rgba(80, 80, 197, 0.4);
    }
    
    /* Footer */
    .footer {
      background-color: #f5f5f9;
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #8a8a8a;
    }
    
    /* Pulse animation for attention */
    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); }
    }
    
    .highlight {
      color: #2a2a72;
      font-weight: bold;
    }
    
    .animated-box {
      border: 2px dashed #5050c5;
      padding: 15px;
      margin: 20px 0;
      border-radius: 8px;
      background: rgba(80, 80, 197, 0.05);
      animation: pulse 2s infinite;
    }
    
    /* Timer animation */
    .timer-container {
      text-align: center;
      margin: 20px 0;
    }
    
    .timer {
      font-size: 14px;
      color: #6a6a6a;
    }
    
    .timer-bar {
      height: 6px;
      background: #e0e0e0;
      border-radius: 3px;
      margin: 10px 0;
      overflow: hidden;
      position: relative;
    }
    
    .timer-progress {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      background: linear-gradient(90deg, #2a2a72, #5050c5);
      transform-origin: left;
      animation: countdown 86400s linear forwards;
    }
    
    @keyframes countdown {
      from { transform: scaleX(1); }
      to { transform: scaleX(0); }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header -->
    <div class="header">
      <!-- Animated particles -->
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      
      <!-- Logo (replace with your actual logo) -->
      <img src="https://via.placeholder.com/180x60?text=VOTEREUM" alt="Votereum Logo" class="logo" />
      
      <h1 class="header-text">Password Reset</h1>
    </div>
    
    <!-- Main content -->
    <div class="content">
      <h1>Forgot Your Password?</h1>
      
      <p>Hi {{ name }},</p>
      
      <p>We received a request to reset the password for your Votereum account. No worries - it happens to the best of us!</p>
      
      <div class="animated-box">
        <p style="margin-bottom: 0;">Account email: <span class="highlight">{{ email }}</span></p>
      </div>
      
      <p>To reset your password, click the button below. This link will expire in 24 hours for security reasons.</p>
      
      <!-- CTA Button -->
      <a href="{{ reset_url }}" class="button" style="display: block;">
        Reset Your Password
      </a>
      
      <!-- Timer visualization -->
      <div class="timer-container">
        <div class="timer-bar">
          <div class="timer-progress"></div>
        </div>
        <p class="timer">This link expires in 24 hours</p>
      </div>
      
      <p>If you didn't request a password reset, please ignore this email or contact our support team if you have any concerns.</p>
      
      <p>Best regards,<br>The Votereum Team</p>
    </div>
    
    <!-- Footer -->
    <div class="footer">
      <p>© 2025 Votereum. All rights reserved.</p>
      <p>This email was sent to {{ email }}. If you believe this was a mistake, please contact support.</p>
    </div>
  </div>
</body>
</html>